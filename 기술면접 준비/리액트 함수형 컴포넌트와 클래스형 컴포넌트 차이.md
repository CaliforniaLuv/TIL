```

  기존에는 클래스형을 사용했으나, 16.8 버전부터는 함수형이 등장하며 해당 방법을 사용하라고 공식문서에서 권장 중이다.
  
  클래스형 컴포넌트
  1. class 키워드 필요
  2. component로 상속 필요
  3. render() 메서드를 통해 HTML 구성
  4. state, lifecycle 관련 기능 사용 가능
  5. 함수형보다 메모리 자원을 더 사용한다.
  
  함수형 컴포넌트
  1. state, lifecycle 관련 기능 사용 못함(대신 React Hook으로 사용)
  2. 클래스형보다 메모리 자원을 덜 사용

  가장 큰 차이는 props이다.
  클래스형은 this.으로 props를 지정하여 사용하는데,
  이는 상태성 변경이 가능하다. 다시 렌더링 시 this.props가 바뀐다.(그러나 건들지 않는 이상 큰 영향은 없지만 엄연히 변경 현상이 일어)
  
```
